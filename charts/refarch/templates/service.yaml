apiVersion: v1
kind: Service
metadata:
  name: {{ include "refarch.fullname" . }}
  labels:
    {{- include "refarch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "refarch.selectorLabels" . | nindent 4 }}
