apiVersion: v1
kind: Service
metadata:
  name: {{ include "eai.fullname" . }}
  labels:
    {{- include "eai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eai.selectorLabels" . | nindent 4 }}
