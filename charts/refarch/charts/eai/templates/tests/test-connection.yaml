apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eai.fullname" . }}-test-connection"
  labels:
    {{- include "eai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
