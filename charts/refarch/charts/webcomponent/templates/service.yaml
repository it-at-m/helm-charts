apiVersion: v1
kind: Service
metadata:
  name: {{ include "webcomponent.fullname" . }}
  labels:
    {{- include "webcomponent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webcomponent.selectorLabels" . | nindent 4 }}
