apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webcomponent.fullname" . }}-test-connection"
  labels:
    {{- include "webcomponent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webcomponent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
