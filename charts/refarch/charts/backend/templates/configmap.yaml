apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.fullname" . }}
  labels:
    {{- include "backend.labels" . | nindent 4 }}
data:
  {{- with .Values.applicationYML }}
  application.yml: |
    {{- toYaml . | nindent 4 }}
  {{- end }}
