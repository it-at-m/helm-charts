apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ad2image-helm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ad2image-helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      #      args: ['--help']
      args:
        - ["--header 'Authorization: Basic Y2x1c3RlcjpjbHVzdGVy'"]
        - ['{{ include "ad2image-helm-chart.fullname" . }}:{{ .Values.service.port }}']
  # Fails with ' 401 authentication failed'
  #      args:
  #        - ['{{ include "ad2image-helm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never