# Additional values for dave-selfservice-portal for openShift environments.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

securityContext:
  {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000
