apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "daveFrontend.fullname" . }}
  labels: 
    {{- include "daveFrontend.labels" . | nindent 4 }}
data:
  {{- with .Values.applicationYML }}
  application.yml: |
    {{- toYaml . | nindent 4 }}
  {{- end }}
