apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "daveSelfservicePortal.fullname" . }}
  labels:
    {{- include "dave.labels" . | nindent 4 }}
  {{- with .Values.daveSelfservicePortal.deploymentAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if not .Values.daveSelfservicePortal.autoscaling.enabled }}
  replicas: {{ .Values.daveSelfservicePortal.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "dave.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap-dave-selfservice-portal.yaml") . | sha256sum }}
        {{- with .Values.daveSelfservicePortal.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "dave.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.daveSelfservicePortal.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "dave.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.daveSelfservicePortal.podSecurityContext | nindent 8 }}
      volumes:
        - name: config
          configMap:
            {{- if .Values.daveSelfservicePortal.existingConfigMap }}
            name: {{ .Values.daveSelfservicePortal.existingConfigMap }}
            {{- else }}
            name: {{ include "daveSelfservicePortal.fullname" . }}
            {{- end }}
            defaultMode: 420
      {{- with .Values.daveSelfservicePortal.extraVolumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- with .Values.daveSelfservicePortal.initContainers }}
        {{- toYaml . | nindent 8}}
        {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.daveSelfservicePortal.securityContext | nindent 12 }}
          image: "{{ .Values.daveSelfservicePortal.image.repository }}:{{ .Values.daveSelfservicePortal.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.daveSelfservicePortal.image.pullPolicy }}
          env:
            - name: LC_ALL
              value: C.utf8
          {{- with .Values.global.extraEnvVars }}
            {{- toYaml . | nindent 12 }}
          {{- end}}
#          envFrom:
#            - prefix: DAVE_LDAP_
#              secretRef:
#                name: {{ .Values.daveSelfservicePortal.credentials.existingSecret }}
          ports:
            - name: http
              containerPort: {{ .Values.daveSelfservicePortal.service.port }}
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /deployments/config
          {{- with .Values.daveSelfservicePortal.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
#          livenessProbe:
#            httpGet:
#              path: /actuator/health/liveness
#              port: http
#            initialDelaySeconds: 60
#            timeoutSeconds: 15
#            periodSeconds: 10
#            successThreshold: 1
#            failureThreshold: 5
#          readinessProbe:
#            httpGet:
#              path: /actuator/health/readiness
#              port: http
#            initialDelaySeconds: 10
#            timeoutSeconds: 5
#            periodSeconds: 5
#            successThreshold: 1
#            failureThreshold: 20
          resources:
            {{- toYaml .Values.daveSelfservicePortal.resources | nindent 12 }}
      {{- with .Values.daveSelfservicePortal.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.daveSelfservicePortal.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.daveSelfservicePortal.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
