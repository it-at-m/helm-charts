apiVersion: v1
kind: Service
metadata:
  name: {{ include "daveAdminPortal.fullname" . }}
  labels:
    {{- include "dave.labels" . | nindent 4 }}
spec:
  type: {{ .Values.daveAdminPortal.service.type }}
  ports:
    - port: {{ .Values.daveAdminPortal.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dave.selectorLabels" . | nindent 4 }}
