apiVersion: v1
kind: Service
metadata:
  name: {{ include "daveFrontend.fullname" . }}
  labels:
    {{- include "dave.labels" . | nindent 4 }}
spec:
  type: {{ .Values.daveFrontend.service.type }}
  ports:
    - port: {{ .Values.daveFrontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dave.selectorLabels" . | nindent 4 }}
