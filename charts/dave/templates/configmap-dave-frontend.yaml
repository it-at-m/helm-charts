apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "daveFrontend.fullname" . }}
  labels: 
    {{- include "dave.labels" . | nindent 4 }}
data:
  {{- with .Values.daveFrontend.applicationYML }}
  application.yml: |
    {{- toYaml . | nindent 4 }}
  {{- end }}
