apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "refarch-eai.fullname" . }}-test-connection"
  labels:
    {{- include "refarch-eai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "refarch-eai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
